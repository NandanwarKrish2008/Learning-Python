# Tuples in python are immutable sequences, typically used to store collections of heterogeneous data. They are defined using parentheses () and can contain elements of different types.


numbers = (1, 2, 3, 4, 5); # defines a tuple of numbers
print(numbers); # prints the entire tuple

